<ul class='dash-ul'>
  <% @users.each do |user| %>
    <%= content_tag_for :li, user, class: 'dash-li' do %>
      <%= link_to user.name, user_path(user.id), class: 'dash-link' %>
    <% end %>
  <% end %>
</ul>